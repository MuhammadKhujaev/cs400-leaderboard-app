stages:
    - submission feedback
submission feedback:
    stage: submission feedback
    tags: 
        - csl
    image: registry.doit.wisc.edu/cdis/cs/courses/cs400/containers:latest


    script:
        - chmod a+x .checkSubmission

    timeout: 5m
    script:


    timeout: 5m
    script:


    script:
        - chmod a+x .checkSubmission

        - ./.checkSubmission
